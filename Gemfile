source "https://rubygems.org"

gemspec

# This section is for dev dependencies that only used by the main Rake
# invocation and the main RSpec invocation, but not by version-specific
# RSpec invocations (utilizing gemfiles/*).
#
# Dev dependencies used by all those locations belong in the gemspec.
# Dev dependencies used by version-specific RSpec invocations belong in gemfiles/*.

gem "rake"
gem "shellwords"
gem "standard"

gem "rspec", "~> 3.13"
