source "https://rubygems.org"

gemspec

# This file is for dev dependencies that are only used by the main RSpec+Rake
# invocation (via Rake), but not by version-specific RSpec+Rake invocations
# (those utilizing gemfiles/*).
#
# Dev dependencies used by all those locations belong in the gemspec.
# Dev dependencies used by version-specific RSpec+Rake invocations belong in gemfiles/*.

gem "standard"
